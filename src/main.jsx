import React from 'react';
import ReactDOM from 'react-dom/client';
import { GifUniverse } from './GifUniverse';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode >
        <GifUniverse />
    </React.StrictMode>,
)